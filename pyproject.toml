[project]
name = "detector-de-somnolencia"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
	"fastapi>=0.111.0",
	"uvicorn>=0.30.0",
	"pydantic>=2.7.0",
	"mediapipe>=0.10.0",
	"numpy>=1.24.0",
	"django>=5.0.0",
]

[tool.vercel]
entrypoint = "backend.main:app"
