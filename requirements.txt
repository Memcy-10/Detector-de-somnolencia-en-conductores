# ── Dependencias del proyecto ─────────────────────────────────────
# Backend (FastAPI + MediaPipe)
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0

# ML / Vision
mediapipe>=0.10.0
opencv-contrib-python-headless>=4.8.0
numpy>=1.24.0

# Frontend (Django)
django>=5.0.0
